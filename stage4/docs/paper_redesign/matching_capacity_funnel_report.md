# Matching-capacity funnel status

**NOT_RUN — prerequisite B failed.** Taskbook B5 requires stopping on a candidate-identity difference before continuing mechanism interpretation. No gate-capacity or K-sensitivity conclusion is inferred from the earlier opportunity percentages.

The first neutral probe supplies one useful bounded diagnostic: E increases 109 -> 110 while U=25 and maximum matching M=24 remain unchanged. This demonstrates that an edge-count change need not change capacity, but does not establish how the substantive passenger/structural/evidence/patience gates behave.

K=10/20/40/80 sensitivity: NOT_RUN. No K20_CAPACITY_STABLE or K20_CAPACITY_SENSITIVE classification is assigned. Canonical K remains 20.

Deadline-level ever-eligible order share: NOT AVAILABLE FROM FROZEN LOGS inspected here. The available frozen ablation products contain aggregate counts and selected assignments, not every solver-eligible AV option over each order's entire waiting history. Selected logs cannot establish absence of an unselected feasible option.

The small graph_metrics helper uses SciPy sparse maximum bipartite matching and returns E/U/M from the exact same deduplicated edge set. Its focused test verifies both redundant-edge loss and capacity loss. Full gate analysis remains pending the identity-policy closure.
