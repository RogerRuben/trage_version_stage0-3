# Operational zone definition

Operational zones use a fixed longitude/latitude grid (`operational_zone_grid_v1`).
They are used for supply statistics, pickup ETA calibration, request-time
diagnostics, and pickup ODD proxy construction.

Depot clusters are separate from operational zones.  Depots initialize AV
locations only and are not reused as the operating zone system.

